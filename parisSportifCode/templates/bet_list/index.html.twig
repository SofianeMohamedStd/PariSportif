{% extends 'base.html.twig' %}

{% block title %}Hello BetListController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>Liste des Paris en cours</h1>
    </br>
    {% if user is not empty %}
    <a>ID utilisateur : {{ user.id }}</a>
    </br>
    <a>Solde actuel : {{ credit }}</a>
    </br>
    {% for eachbet in betsUser %}
    </br>
    <a>Pari en cours : {{ eachbet.bet.nameBet }}, pour l'évènement : {{ eachbet.bet.evenement.name }} </a>
    </br>
    <a>Montant parié : {{ eachbet.amountbet }}, gain possible : {{ eachbet.gainpossible }} </a>
    </br>
    <a href="betmodifier{{eachbet.id}}">Modifier ou supprimer le pari</a>
    {# <a>{{eachbet.id}} </a> #}
    </br>
    {% endfor %}
    {% endif %}
    
    {# {% for bet in bets %}
        <a>{{ bet.id }} Montant : </a>
        {# {{ bet.amount }} #}
        {# </br>
    {% endfor %} #} 
    </br>
    <a href="{{ path('index_user') }}">Retour à l'accueil</a>
</div>
{% endblock %}
